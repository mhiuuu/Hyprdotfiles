(defwidget colorpicker [] 
    (box :class "button-bin"
        (button
            :class "button colorpicker"
            :onclick "hyprpicker"
            (label :class "icon" :text "󰈊")
        )
    )
)
